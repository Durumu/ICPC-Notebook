#include "main.cpp"
using namespace std;

// A line is stored parametrically, simplifying calculations
struct Line{
  // position vector
  PT pos;
  // direction vector
  PT dir;
};

PT intersect(Line a, Line b) {
  //
}

int main() {
  cout << eq(6.5, 6.5) << endl;
}
